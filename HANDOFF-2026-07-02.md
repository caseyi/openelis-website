# Website rebuild — handoff (2026-07-02)

Pick this up in a fresh chat (on Fable). Everything below is committed to disk under
`/Users/casey/Documents/OpenELIS Website/openelis-website/`. Nothing is in git yet.

## What this cycle decided
- **Leave WordPress; keep Bluehost.** Static HTML built from this repo, SFTP'd to the same Bluehost
  account. Cutover is a docroot swap — no DNS/host change.
- **GTranslate survives the move.** `fr.openelis-global.org` (and other language subdomains) are
  served by GTranslate's paid proxy, independent of WordPress. Multilingual SEO is preserved; the
  static template just needs the GTranslate JS snippet + hreflang tags (hreflang already emitted).
- **Full scope, staged quick-wins-first.** See `workplan/website-refresh-plan-2026-07.md` (6 stages).
- **One Health is the headline story** — human + animal/vector + environmental in one LIMS.

## What's built and verified (localhost preview)
- **Static pipeline:** `site/` (source) → `node site/build.mjs` → `dist/`.
  - `site/_layout/base.html` — shell: nav, footer, per-page OG tags, hreflang, schema.org, GTranslate mount.
  - `site/_assets/site.css` — shared stylesheet (teal/orange palette from design-conventions).
  - `site/_assets/openelis-logo.png` + `-white.png` — real logo (from Casey's Downloads).
  - `site/build.mjs` — wraps page fragments, generates sitemap.xml + llms.txt. Skips `_incoming/`, `.webm`, `.json`.
  - `site/pages/index.html` — homepage: hero, canonical stat strip, audience router, **One Health band**, "see it in action", CTA.
  - `site/pages/capabilities/environmental-testing/index.html` — full capability page, real screenshots, demo/manual/GitHub router, One Health cross-link.
- **Screenshots:** curated shots in `assets/screenshots/{environmental-testing,homepage}/`.
  140+ more triaged in `assets/screenshots/_incoming/` (not shipped by the build).

## Screenshot harness notes (important)
- Harness lives at `~/Documents/OpenELIS QA/` (skill: openelis-screenshots). Runs on the Mac only.
- **testing.openelis-global.org has NO environmental data** and its Laporan Hasil search endpoint
  404s. **Environmental/vector/Laporan Hasil live on `indonesiademo.openelis-global.org`** — capture
  those with `BASE=https://indonesiademo.openelis-global.org`.
- Full 78-spec run passed (2026-07-02). Pathology/IHC/Cytology captured but dashboards were empty
  (0 cases) — seed demo cases before re-capturing those for the site.

## Preview command (Mac)
```
cd ~/Documents/OpenELIS\ Website/openelis-website && node site/build.mjs
cd dist && python3 -m http.server 8899   # then open http://localhost:8899
```

## Update — later same day (One Health restructure)
- **IA changed to One Health hub-and-spoke.** New flagship `/capabilities/one-health/` (built) replaces
  Environmental as headline. Homepage One Health band is now a teaser linking to the hub. Environmental
  is a spoke. Nav gained **One Health** + **Why OpenELIS**.
- **New decision-maker page `/why-openelis/`** (built): TCO/COCOMO, sovereignty, proven-at-scale
  (Mauritius), JMIR evidence, security/accreditation, interop, governance. Audience router's
  "Evaluating for your country" now points here.
- **COCOMO dollar figures** (~$33.9M / ~$67.7M / 376 person-years) on the Why page are **approved for
  public use** (Casey, 2026-07-02). Citation line to Jan Flowers / DIGI retained.
- **⚠ Vector has no clean screenshot.** The vector identification/surveillance views 404 or need seeded
  data on BOTH testing and indonesiademo. One Health hub uses icon cards + a 2-shot "See it in OpenELIS"
  band (clinical QC + env compliance) instead. To finish: seed a vector pool + identification case on the
  Indonesia demo, re-capture `tests/docs/vector-surveillance.docs.spec.ts`, add a third tile.
- Build now emits **4 pages**: `/`, `/capabilities/one-health/`, `/capabilities/environmental-testing/`, `/why-openelis/`.
- Decision still open: does clinical get its own spoke page, or does Features & Functionality serve as it?

## Update 2 — two-axis taxonomy + compliance reframe
- **Two-axis capability model** (`docs/capability-taxonomy.md`): Axis 1 = domains (One Health: human /
  vector / environmental); Axis 2 = cross-cutting platform capabilities (QC, FHIR, security, storage,
  reporting, compliance eval, analyzer, inventory) that serve all domains. No separate clinical spoke —
  **Features & Functionality is the clinical + platform page** (built at `/features-and-functionality/`,
  capability cards badged ALL DOMAINS vs CLINICAL-FOCUSED).
- Each domain spoke gets a **"Powered by the OpenELIS platform"** strip (built on the environmental spoke
  as the pattern) linking to the cross-cutting capabilities.
- **Compliance reframe (Casey):** the "environmental" spoke is really **Regulatory & Compliance Testing** —
  evaluate any result against any regulation: environmental, **food safety, pharmaceutical/drug, product/
  technical — anything measured against a standard.** Page retitled; hero = "Test anything. Evaluate it
  against any regulation." Added "One engine, many regulated domains" section + a compliance-evaluation
  platform-capability card on Features. **Slug still `/environmental-testing/`** — rename candidate
  `/regulatory-testing/` (Casey to decide). Flagged: this capability reaches beyond One Health.
- **Seeding is being handled in another thread** (Casey has the automation). Underwhelming/empty
  screenshots (clinical results, dashboards, vector, pathology) will be re-captured once data is seeded.
- Build now emits **5 pages**: `/`, `/features-and-functionality/`, `/capabilities/one-health/`,
  `/capabilities/environmental-testing/`, `/why-openelis/`.

## Update 3 — Features deep-port, icons, copy
- **Features & Functionality fully ported** to static (`/features-and-functionality/`): pitch intro
  ("Everything the lab does — connected" — workflow + analyzer + lab-network/HIE, no jargon),
  effusive overview cards (jump-links), deep sections grouped clinical-lifecycle vs platform, each with
  bullets + **"Why it matters" evidence callout** + real Dec-2025 screenshots (pulled to
  `assets/screenshots/features/`, self-hosted). Proven-at-scale + Catalyst (roadmap-framed) + CTA.
- **Icon system:** all emoji replaced with **Carbon Design System icons (Apache 2.0)** in
  `site/_assets/icons/` (21 SVGs), recolored via CSS mask (`.ic` class). On-brand with the product UI.
  No emoji remain anywhere in the pages.
- **Image sourcing decision:** Carbon icons for UI; Unsplash/Pexels (no attribution) or Wikimedia
  Commons (CC BY) for any future photography — but favor real screenshots.
- Layering principle (Casey): every level is a complete pitch; detail/evidence builds as you go deeper
  (overview card → section bullets → why-it-matters → screenshot → spoke page).

## Next steps (in priority order)
1. **Commit** to a branch (`feat/static-rebuild`) per repo conventions — nothing is in git yet.
2. Build remaining 4 capability pages from their `pages/capabilities/*/COPY-DRAFT.md` using the
   same pattern as environmental-testing (Analyzer, Vector, Inventory, Sample & Storage).
3. Port the existing WP pages 1:1 (Features, About cluster, Getting Started, Published Works —
   **de-dupe the duplicated "Related Research" section**), fixing stale dates + Westgard contradiction.
4. Confirm real `docs.openelis-global.org` user-manual URLs (I guessed the env slug — may 404).
5. Provide a **canonical stats block** (one set of numbers) — currently 1,000+/18.7M+/26+/15+/$0.
6. Decide hero-CTA pattern (carried from April).
7. Seed pathology/IHC/cytology demo cases, re-capture, add those pages.
8. GitHub Action → SFTP to Bluehost staging subdomain.

## Open questions for Casey
- Exact docs user-manual URLs per feature.
- Canonical stats numbers.
- Staging subdomain name + Bluehost SFTP creds (out-of-band).
- Repo move `caseyi/openelis-website` → `DIGI-UW`?
