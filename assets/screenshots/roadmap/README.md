# Roadmap page screenshots

**Captured:** _not applicable — Roadmap page does not require new screenshots_
**Reference brief:** `docs/screenshot-brief.md` §6.7

## Strategy

The Roadmap page uses icons + status badges, not screenshots. The "Recently Shipped" tier reuses thumbnails from the corresponding feature pages — see `assets/screenshots/inventory-management/` and `assets/screenshots/sample-storage/`.

If a Tier 1 (Recently Shipped) capability lands without a feature page (e.g., Patient Photo Capture before its page is built), capture a single representative thumbnail here and follow the standard naming convention from §3 of the brief.

## Notes

- This directory exists to keep the per-feature directory pattern consistent — even if it stays empty, having the README documents the decision.
